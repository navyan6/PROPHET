# Core Python packages for binding affinity + tree analysis

# PyTorch and deep learning (GPU support for CUDA 12.4)
numpy<2
torch==2.4.0
torchvision==0.18.0
torchaudio==2.4.0

# Transformers — pinned to match PeptiVerse's tested version
transformers==4.46.0

# Scientific computing
scipy>=1.11.0
scikit-learn>=1.3.0
pandas>=2.0.0
matplotlib>=3.8.0
seaborn>=0.13.0
tqdm>=4.66.0

# Bioinformatics & ML
biopython>=1.82
xgboost>=2.0.3
joblib>=1.3.0
rdkit>=2023.3.1

# HuggingFace ecosystem
huggingface_hub>=0.20.0

# Lightning (new package name for pytorch-lightning >= 2.0)
lightning>=2.1.0

# PeptiVerse SMILES tokenizer
SmilesPE

# Development (optional)
pytest>=7.4.0
black>=23.12.0
flake8>=6.1.0

# ============================================================================
# EXTERNAL SYSTEM TOOLS (not pip packages - install via conda/homebrew)
# ============================================================================

# MAFFT (multiple sequence alignment)
#   conda install -c bioconda mafft
#   or: brew install mafft
#   Required by: tree_analysis/src/phylogeny.py

# FastTree (phylogenetic tree inference)
#   conda install -c bioconda fasttree
#   Required by: tree_analysis/src/phylogeny.py

# ============================================================================
# INSTALLATION NOTES
# ============================================================================

# macOS users (Intel + Apple Silicon):
#   brew install mafft fasttree libomp
#   pip install -r requirements.txt
#   For XGBoost compatibility: export LDFLAGS=-L/opt/homebrew/opt/libomp/lib

# Linux GPU users (NVIDIA CUDA 12.4):
#   conda install -c bioconda mafft fasttree
#   pip install torch==2.4.0 torchvision==0.18.0 torchaudio==2.4.0 \
#       --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt
#   Verify GPU: python -c "import torch; print(torch.cuda.is_available())"
